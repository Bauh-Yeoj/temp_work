#!/bin/bash
url=https://server.nippon.workers.dev/ccache/ci2/styxysl_ccache.tar.gz

# General setup
sudo apt -y update
sudo apt -y install aria2 bc ccache rclone rsync

cd /tmp # Where to download cccahe
time aria2c $url -x16 -s50 || time aria2c $url -x16 -s50
time tar xf styxysl_ccache.tar.gz
find styxysl_ccache.tar.gz -delete
